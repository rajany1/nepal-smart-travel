# 2026-08-17

## REVIEW 404 — ROOT CAUSE FOUND & FIXED (the REAL one)
- AccessLog middleware (`app/Http/Middleware/AccessLog.php`, logged in bootstrap/app.php web+api prepend) revealed the phone's POST: `POST api/v1/places/admin_3705/reviews → 404`
- Place ids from `nearbyCombined` are **source-prefixed**: DB places → `'id' => 'admin_'.$place->id` (PlaceController:374), OSM → `'id' => 'osm_'.$osmId` (:613)
- `addReview`/`reviews`/`show` only handled `osm_` → `admin_3705` fell into `findOrFail` → 404. THE user's whole week of "404" = this.
- Fix: `PlaceController::resolveDbId()` — strips `admin_` prefix; applied in addReview (findOrFail), reviews (where place_id), show (else branch). Also translated GET /places/{id} + GET /places/{id}/reviews → both 200 now.
- Verified: GET show 200, GET reviews 200, POST (real token) 200, review stored approved, cleanup done.
- The earlier "osm_"/uuid fixes were real but the app rarely hits those paths (OSM places get auto-created on review anyway).

## Routes overlay (committed 5cbc36a)
- MapViewProvider.toggleRoutes, ApiClient.getRoutes(withTrack:), nearby map hiking FAB + polylines/markers, route list screen.

## REVIEW LIST ROUTE-SHADOWING BUG (fixed after user said "review herna ta pauna paryo")
- GET /places/admin_3705/reviews returned the PLACE object, not the reviews array!
- Cause: `GET /places/{id}` (regex `.*`) was registered BEFORE `GET /places/{id}/reviews` → `{id}` swallowed `admin_3705/reviews` → show() ran. Route order matters: specific sub-paths MUST be registered before `{id}`.
- Fix: api.php reorder — reviews + translations now BEFORE /places/{id}. POST was unaffected (different method/shape).
- NOTE: with `.*` wildcard, MySQL int coercion also made show() return places for numeric "1/reviews" — silent wrong data. Always verify response BODY, not just status code (my earlier "200" checks missed this!).
- App side needed no change: fetchPlaceReviews parses `data as List`; was failing silently on the place object.
- Verified: admin_3705 reviews → array [test], admin_3717 → [test2], places/1/reviews → [], show → place. User's server (PID 18788) picked up route changes automatically (no route cache; opcache validate_timestamps).

## DIRECTIONS — NO GOOGLE MAPS, backend proxy (after user: "google map hamro project ma hunai hudaina")
- Original logic (OSRM in-app polyline, from git history — same since 99db02c) was fine but called router.project-osrm.org DIRECTLY from the phone → unreliable from phone networks.
- Fix: `PlaceController::directions()` — `GET /routing/directions?from_lat=&from_lng=&to_lat=&to_lng=` → server fetches OSRM (User-Agent header, timeout 15s) → clean `{success, routes:[{points:[{lat,lng}], distance, duration}]}` → Cache::remember 15 min → 502 JSON on upstream failure. Route added api.php public group. Verified HTTP 200 on user's server (675 pts, 5.68km) — no restart needed (no route cache, opcache revalidates).
- App: `ApiClient.getDirections()`; nearby map + places `_getDirections`/`_fetchDestinationRoute` use it; report map screen `_openDirections` draws in-app polyline + fitCamera (no external app); place_details `_openDirections` → `NearbyPlacesScreen.withDestination()` (in-app route, NOT external).
- Google Maps URLs DELETED everywhere (map/list/report/detail + destination-preview "Open Maps" → "Clear" button). Unused url_launcher/http/dart:convert imports removed (nearby map/list). flutter analyze errors 0.
- Remaining url_launcher uses: website/phone/email (details screen) + report links — those now work because manifest `<queries>` added https/tel/mailto/geo (Android 11+ package visibility was silently killing ALL launchUrl/canLaunchUrl before — that was the real "Directions buttons do nothing" cause for launchUrl flows).


## Review pipeline (earlier today)
- PlaceReview uuid creating hook; route `where('id','.*')`; direct publish (moderation_status='approved', ModerateReview dispatch removed); admin reviews CRUD (placeReviews/updatePlaceReview/deletePlaceReview + recalcPlaceRating `?? 0`); places.blade.php 💬 modal; app: Details button, 401→LoginScreen redirect, URL removed from snackbar + debugPrint added.

## Gotchas (again, they bit us)
- opcache.enable_cli=On → restart php artisan serve (kill port 8000 PID) after ANY php edit. Server now PID 6596 (hidden process).
- Production api.nepalsmarttravel.com DOES NOT RESOLVE (DNS) — app CANNOT hit prod; all traffic is local. Release builds would fail, but user's phone hits the PC fine (Dart/3.7 UA in access log).
- AccessLog middleware is temporary diagnostic — REMOVE when done (or keep, it's cheap; it proved invaluable).
- Test tokens: `User::find(2)->createToken('diag')` then curl Bearer; delete after (`tokens()->delete()`).

 — Curated Routes → Trekking + Flutter App

## What was built

User asked: make curated routes work for TREKKING + the original plan (itineraries), and build it in the Flutter app with map integration.

### Backend (Laravel)
- **Migration** `2026_08_17_000000_add_trekking_fields_to_curated_routes`: `route_type` (itinerary|trekking, index), `difficulty` (easy|moderate|challenging|hard, index), `max_altitude_m`, `total_distance_km`, `elevation_gain_m`, `starting_point`, `ending_point`, `track` (json — array of `{lat,lng,name}`).
- **CuratedRoute model**: fillable/casts updated + `trackPoints()` (accepts `{lat,lng,name}` maps OR `[lat,lng]` pairs), `difficultyLabel()`, scopes `type()`/`difficulty()`, consts TYPES/DIFFICULTIES.
- **API**: `App\Http\Controllers\Api\RouteController` — `GET /api/v1/routes` (filters: type, difficulty, q; limit) + `GET /api/v1/routes/{id}` (adds `waypoints`, `track`, `places[]` with lat/lng/rating/category/image). Registered in api.php BEFORE auth group (public).
- **Admin CRUD**: controller validation + blade modal got: route type select, difficulty select, max altitude, distance, elev gain, start/end point, track textarea ("lat,lng,Name" per line or JSON). Edit JS loads track back to textarea.
- **Seeder** `CuratedRouteSeeder` (hooked into DatabaseSeeder): EBC Trek (hard, 14d, 5644m, 130km, 17 GPS points), ABC Trek (moderate, 8d, 4130m, 110km), Poon Hill (easy, 5d, 3210m, 55km), + KTM Valley Heritage Circle & Pokhara Lakeside (itineraries). updateOrCreate by slug — safe re-seed.
- **Web**: routes list cards show Trekking/Itinerary badge + difficulty/distance/altitude chips; route detail adds stat grid (distance, altitude, elevation, start→end) + **Leaflet map** (OSM tiles, polyline + numbered markers, `@push('head')`/`@push('scripts')` — NOTE layout has NO `@stack('styles')`, only 'head') + numbered trail waypoint list.

### 🐛 BOM BUG FOUND & FIXED (important!)
- `backend/routes/api.php` had a **UTF-8 BOM** at byte 0 → PHP echoed it on EVERY request → every API response started with `EF BB BF` → would break Dart `jsonDecode` in the app (silently broke offers/places too). Removed BOM. Scanned all non-vendor PHP files: 0 remaining.

### Flutter
- `core/models/route_model.dart`: `CuratedRouteModel`, `RouteTrackPoint`, `RoutePlaceModel`.
- `core/api/api_client.dart`: `getRoutes({type,difficulty,search,limit})`, `getRouteById(id)`.
- `providers/route_provider.dart`: fetchRoutes (filters), fetchRouteDetail, registered in main.dart MultiProvider.
- `features/routes/routes_screen.dart`: filter chips (All/Trekking/Itineraries + difficulty), route cards (gradient header, badges, stats), RefreshIndicator.
- `features/routes/route_detail_screen.dart`: gradient header w/ stats, flutter_map (OSM tiles) polyline + numbered markers from track, stops (→ PlaceDetailsScreen), trail waypoints list. Named route `/routes` in main.dart.
- Home screen: "Routes" quick action (Icons.hiking) + "Routes & Treks" horizontal strip section (`_RoutesStrip`/`_MiniRouteCard`).
- NearbyMapScreen: "Routes" button under map-mode toggle (top-left, below it).

### Gotchas
- `Icons.route_off` doesn't exist → use `Icons.map_outlined`.
- flutter_map v7: Marker width/height + child widget for numbered markers works fine.
- Track points stored as JSON objects `{lat,lng,name}` (not lat-lng pairs) — cleaner for both web Leaflet + Dart.
- `track` cast 'array' → `trackPoints()` guards empty.
- Admin track textarea uses comma-join `p.lat,p.lng,p.name`.

## Verified
- migration ran, seeder ran (5 routes), API list/detail 200 w/ track JSON, BOM gone (first byte 0x7B), web list+detail pages render w/ Leaflet, admin routes page redirects to login, `flutter analyze` — 0 errors on all touched files (446 pre-existing issues elsewhere), php -l clean on all touched files.

## Follow-up (same day): trekking routes ON the nearby map
- Backend: `GET /api/v1/routes?with_track=1` now includes `track[]` in list response (`RouteController::publicRoute($route, $withTrack)`).
- Flutter: `MapViewProvider.showRoutes` + `toggleRoutes()`; `ApiClient.getRoutes(withTrack:)`.
- `nearby_map_screen.dart`: hiking FAB (amber when ON) between weather & add-place FABs → lazy-fetches overlays once (`_loadRouteOverlays`, keeps only track>1), draws amber polyline per trekking route (teal for itineraries) + green flag start / red `sports_score` end markers; marker tap → RouteDetailScreen. `_buildFlutterMap` gained `showRoutes` param.
- Verified: API returns 3 trekking routes with track; flutter analyze 0 errors on touched files. NOT pushed yet.

## Fix (same day): review submission failing "Failed to submit review"
- Root cause #1: `place_reviews.uuid` column has NO db default and `PlaceReview` model never generated one → MySQL error 1364 on every insert → HTTP 500. Fixed via `PlaceReview::booted()` creating event (`Str::uuid()`), matches Place/User/Report/Alert convention.
- GOTCHA: user's `php artisan serve` has **opcache.enable_cli=On** → long-running server serves STALE bytecode; must restart server after PHP edits (kill PID on port + `Start-Process php -ArgumentList "artisan serve --port=8000" -WindowStyle Hidden`).
- Root cause #2: OSM place ids are `osm_node/12345` (slash inside, from `$element['type'].'/'.$element['id']`). Route `{id}` default regex `[^/]+` didn't match → POST/GET `/places/osm_node/123/reviews` = 404. Fixed: `->where('id', '.*')` on `GET /places/{id}`, `GET /places/{id}/reviews`, `POST /places/{id}/reviews` (auth). Verified 200 for show/reviews/review-POST with osm id; test place cleaned.
- Flutter UX: "Details" button added in both nearby screens' sheet preview (review path was hidden behind tapping card); snackbar now shows status code (e.g. "(404)") or Dio error type for easier debugging; 401 → "Please log in to write a review" + LoginScreen.
- Committed + pushed as 5cbc36a (map overlay). Review fixes NOT pushed yet.

## Direct-publish reviews + admin review management (user request)
- `PlaceController::addReview`: reviews now publish DIRECTLY — `moderation_status='approved'`, `moderated_at=now()`, removed `ModerateReview` job dispatch + its import. Guard (realtime censor) kept; TranslateContent kept. User requested: no pending moderation, admin edits later on request.
- Admin (places list): new `💬` Reviews button per place row → modal (fetch `/admin/places/{id}/reviews` JSON): stars, user, date, status badge, per-review **Edit** (rating/title/description/status approved|rejected) + **Delete**. Routes: `GET admin/places/{id}/reviews`, `POST admin/places/reviews/{id}/update`, `POST admin/places/reviews/{id}/delete` (registered BEFORE `/places/{id}` show). `AdminController::placeReviews/updatePlaceReview/deletePlaceReview` + `recalcPlaceRating()` (avg/count over approvedReviews; **must use `?? 0`** — avg NULL when 0 approved reviews → NOT NULL column crash).
- Verified: POST review → moderation_status 'approved'; admin placeReviews JSON OK; update 302 + row changed; delete 302 + recalc; `view:cache` compiles all blades OK; php -l clean; server restarted (opcache!).
- GOTCHA: admin endpoints tested via script with `Auth::login(admin)` + direct controller calls (no password needed for test). Admin user id 2 = rajendrasubedi10011@gmail.com.
