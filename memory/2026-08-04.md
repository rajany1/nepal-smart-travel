# 2026-08-04 — Logic Audit & Phase 1 Fixes

Ran full logic audit (backend/admin, Flutter, integration) → `LOGIC_AUDIT_FIX_PLAN.md` (73 issues: BE/FL/IN).

## Phase 1 fixes completed

1. **IN-01** Partners list: `partners_list_screen.dart` now handles Laravel paginator shape (`data['data']`); also passes `district` filter that was previously built but never sent.
2. **BE-04/BE-05** Push pipeline: `PushNotificationService::notifyNearbyUsers` param renamed `$body`→`$message` (all 4 callers used named `message:` → fatal Error, swallowed). Added geo bounding-box filter via new nullable `push_tokens.latitude/longitude` (migration `2026_08_04_000001`); tokens w/o location still receive (legacy fallback). `AlertController::store` wrapped push in try/catch (was uncaught 500 after alert created → double XP). Added Log to previously-empty catches (AdminController, ReportController).
3. **FL-01** Sync: `sync_service.dart` now actually calls `_api.createPlace()` (new method with multipart `images[]` from `media_paths`) before `markSyncCompleted`; unknown entity types now `markSyncFailed` instead of silent delete; strips empty phone/website.
4. **FL-02/IN-04** Auth: backend now issues `refresh_token` (30d, name='refresh') on register/login/socialLogin (`AuthController::issueTokenPair`); `/auth/refresh` moved out of `auth:sanctum` group; `refreshToken()` now validates refresh token via `PersonalAccessToken`, rotates, rejects banned/suspended. Flutter interceptor sends refresh token in POST body (not header — header was clobbered by onRequest), stores rotated pair, and 403 handling now only clears session for `ACCOUNT_BANNED`/`ACCOUNT_SUSPENDED`/`requires_logout` (PROFILE_INCOMPLETE keeps session).
5. **BE-02/BE-03** Translation: `TranslateContent` job writes `value` column (was `translated_value` → QueryException) and stores short `translatable_type` (was `App\Models\X::class` → never matched readers). Dispatch sites already use short names — verified.
6. **BE-01** FALSE POSITIVE: `AgentOrchestrator::runPendingReports()` exists (line 62) and `php artisan ai:process-reports` works. No fix.

## Verification
- PHP: all changed files lint clean; route list shows `POST api/v1/auth/refresh` public; migration applied to dev DB.
- Flutter: `flutter analyze` = 0 errors project-wide (pre-existing warnings only).
- Migration `2026_08_04_000001_add_location_to_push_tokens_table` ran on dev DB.

## Notes
- PushToken geo filter includes NULL-location tokens intentionally (existing users won't miss emergency pushes until they re-register with location).
- Next: Phase 2 (data correctness) items 7–18 in LOGIC_AUDIT_FIX_PLAN.md.

## Phase 2 fixes completed (items 7–18)

7. **BE-21/IN-06** `ReportController::index` no longer clobbers `$user` (line 127 reassignment removed) — own pending reports visible on public bearer route.
8. **BE-07** `ConsumerController::createBooking` defaults `booked_at` to now before commission math.
9. **BE-08/BE-24** `AnalyzeReport` job rewritten: `is_duplicate` → reject, `!is_legitimate` → reject (overrides action), AI approval awards `report_approval_xp` (default 10) + increments `approved_reports`, updates ModerationQueue. `ReportManagerHandler` `suggested_priority` default `'not-set'` fixed.
10. **BE-10** `Place::approvedReviews()` relation; `addReview` resets moderation on resubmit, awards `review_xp` (key `review_created`, default 3) only on first creation, recalculates rating from approved reviews only; `show`/`reviews` filtered.
11. **BE-11** `approveReport`/`rejectReport` early-return guards; `rejected_reports` incremented on user.
12. **BE-13** ⚠ audit said migration columns existed — WRONG. Verified `users` table has NO `gender`/`interest`. Created migration `2026_08_04_000002_add_gender_interest_to_users_table` (nullable, guarded by hasColumn) + added both to `User::$fillable`.
13. **BE-14** `isPremium()` now requires active sub to a plan whose slug isn't `free`/null — free users no longer premium (matches `my()`/`features()`).
14. **BE-15** `getNextLevelXp($level)` now returns `xpForLevel($level+1)` (was same-band); admin `userProgress` uses `AchievementService::getLevelProgress()` (was total_xp/nextXp → everyone 100%).
15. **BE-16** Commission unified on admin formula: post-discount amount, `rewardShare = commission * 0.25`, `CommissionTransaction` created — in BOTH `ConsumerController::createBooking` (was pre-discount + 0.5 + no tx) and admin `bookingStore`. `ShopService::applyToBooking` now handles `value_npr` discount type (was 0 for same item).
16. **BE-17** Admin `bookingStore` rejects shop codes already `booking_id`/`consumed_at` (was: consumed codes re-attachable → double discount).
17. **BE-19** `ShopService::purchase()` creates purchases as `pending` (migration default; fulfill/cancel were dead code). `cancel()` now also releases the shop code back (is_used=false etc.) — prevents free code after cancel.
18. **BE-28/BE-29** Already covered by items 9+10 (review_xp + AI approval XP) — verified.

## Verification
- All modified PHP files lint clean (`php -l`).
- Migrations ran: `2026_08_04_000002` applied.
- Fix plan checkboxes 1–18 all `[x]`.
- Next: Phase 3 (Map/UX) items 19–27 — FL-04 dual-map binding first.

## Phase 3 fixes completed (items 19–28)

19. **FL-04** Dual-map binding: `nearby_map_screen.dart` previously mounted standard+satellite FlutterMaps in AnimatedOpacity (both hit-testable, one shared `MapController` → in release the controller bound to the invisible satellite layer). Now: `AnimatedSwitcher` mounts ONLY the active map (no dual tile downloads), each layer has its own controller (`_mapController`/`_satelliteMapController`), and a `_activeMapController` getter reads `MapViewProvider.isSatellite` for all programmatic moves (zoom FABs, my-location, route fitCamera, place tap, viewport bounds).
20. **FL-07** Map filter selections now applied: `_activeFilter` state stores `PlaceFilter`; apply passes `categoryId`/`search` to `fetchNearbyPlaces` (forces re-fetch by nulling lastFetch coords) and `_applyPlaceFilters()` filters markers+bottom-sheet client-side (featured, verified, category-name match, search). Fixed nullable `PlaceModel.category` compare.
21. **FL-12/FL-13** Sync: `SyncService.instance.startMonitoring()` wired in `main()` (connectivity listener + 5-min timer; retry cap already in `markSyncFailed`; unknown ops already fail-visible). Sync-count stream created once in `initState` + `_pollSyncCount` loop started once (was recreated per build → accumulating timers).
22. **FL-03** Report search implemented end-to-end: backend `ReportController::index` accepts `search` (title/content/address/district LIKE); `ApiClient.getReports` + `ReportProvider.fetchReports` accept `search`; screen `_onFilterChanged` debounces 400ms and refetches with search + location; `fetchMoreReports` persists `_searchQuery` so paging works under search.
23. **FL-08** 60s auto-refresh no longer clobbers pagination: `startAutoRefresh` now calls `_pollFetchReports()` which fetches page 1 and MERGES (fresh deduped first, older kept) instead of replacing; `_isFetching` guard unchanged.
24. **FL-05** Real Delete Account: backend `DELETE /users/me` (`AuthController::destroy`) purges social accounts, push tokens, XP transactions, achievements, subscription, purchases; revokes tokens; anonymizes user (name/email/phone/avatar/bio/gender/interest/password → 'Deleted User', status='deleted'). `CheckUserStatus` handles `deleted` → 403 `ACCOUNT_DELETED` + requires_logout. Client: `ApiClient.deleteAccount()`, `AuthProvider.deleteAccount()`, settings screen calls it before navigating to login.
25. **FL-06** Photo-GPS capture wired: `submitReport` now serializes `capture_latitude`/`capture_longitude` into FormData; backend accepts them and falls back to `ExifGpsVerificationService::verifyCaptureCoordinates()` when the image has no EXIF GPS (image_picker strips it); screen passes `_captureLocationService` values.
26. **IN-03** `/profile-setup` now routes to `ProfileCompletionScreen` (completion flow sets `profile_completed=true`); email-verification screen pushes `/profile-completion`; profile avatar pencil → `/profile-edit`; backend `AuthController::update` also auto-sets `profile_completed=true` when bio ≥10 chars arrives on an incomplete account. **Also pulled forward IN-05**: `expertise_regions` added to `AuthController::update` validation (was silently dropped).
27. **IN-02** Password reset: backend `forgotPassword` now returns `reset_token` in the JSON response → app's existing "Enter Reset Code" flow actually proceeds (was always null). NOTE: in production, ship via deep link/email code instead.
28. **IN-07** Place images: `PlaceController::placeImageUrls()` helper returns `asset('storage/...')` absolute URLs at all 3 list/detail serialization sites (was raw relative `places/{id}/...` → 404 in app); matches report media pattern.

## Verification
- `flutter analyze` project-wide: **0 errors** (349 pre-existing warnings/infos).
- All PHP changes lint clean; no new migrations needed.
- Fix plan checkboxes 19–28 all `[x]`.
- Next: Phase 4 (Cleanup) items 29–36 — FL-09/IN-08 email verification flow first.

## Phase 4 (Cleanup) — in progress
### Item 29 — FL-09/IN-08 email verification (DONE)
- Backend: register() now generates+caches OTP (10 min) + sends via Mail::raw (try/catch, Log::warning on fail) via new private sendVerificationOtp(); returns otp in response (dev bridge).
- resendVerification() rewritten: sends OTP email + returns otp.
- me() now exposes email_verified_at (was missing).
- Client: UserModel.emailVerifiedAt (DateTime?) parsed + serialized; _isEmailVerified gates on emailVerifiedAt != null at all 7 sites (was status == 'active' → always true → gate dead).
- register_screen: success → /email-verification when unverified (FL-09 route now reachable), else /profile-completion.
- AuthProvider.lastOtp captured from register + resend responses; email_verification_screen prefills 6 OTP boxes from it (dev bridge, mirrors forgot-password reset_token precedent).
- Verified: php -l clean, flutter analyze 0 errors.

### Item 30 — FL-14/FL-24 assistant (DONE)
- assistant_screen _callAssistantApi: real location via LocationService().getCurrentLocation() (falls back to AppConstants defaults); safe parse: data map-guarded, reply .toString() fallback 'Sorry, ma bujhna sakina.', actions List<Map<String,dynamic>> from whereType<Map>().
- flutter analyze 0 errors.
### Item 31 — FL-10/11/23/25/26 (DONE)
- FL-10: empty-string guards — my_bookings_screen.dart:210+407 ((partner?.name ?? '').isEmpty ? '?' : ...); sponsors_screen.dart:100; store_order_card.dart:95 (empty status → ''); :46 substring already guarded (length check existed).
- FL-11: store_item_card.dart copy now calls Clipboard.setData (import services).
- FL-23: sponsors website chip launches via url_launcher _launchWebsite() with https normalization + error snackbar.
- FL-25: reaction popup now anchored to pressed button RenderBox (localToGlobal vs Overlay) instead of fixed bottom:height*0.5-60; drops CompositedTransformFollower.
- FL-26: reply strip via RegExp('^@name\s*') in _stripReplyPrefix().
- flutter analyze 0 errors.

### Item 32 — FL-15/16/17/18/19/20 (DONE)
- FL-15: missing-location submit now shows error snackbar 'Location unavailable...'.
- FL-16: VERIFIED already fixed in Phase 1 (refresh token sent in request body; interceptor cannot clobber; retry sets new Authorization explicitly).
- FL-17: settings theme load maps 'system' → 'System' (was silently Light).
- FL-18: place id comparisons normalized via .toString() at nearby_map_screen (5 selected-markers + 3 firstWhere on placeId) — fixes admin int vs OSM 'node/123' string ids.
- FL-19: comment submit refresh now scoped to feed location via provider.lastFetchLat/Lng (was lat:null → appended global reports to near-you feed).
- FL-20: dedicated emergency list — backend ReportController::index is_emergency filter (whereIn priority high/critical); provider emergencyReports + fetchEmergencyReports/fetchMoreEmergencyReports with own pagination + 60s poll refresh + search; _EmergencyReportsTab stateful w/ ScrollController infinite scroll.
- flutter analyze 0 errors, php -l clean.
### Item 33 — FL-21/22/27/28/29/30/31/32 (DONE)
- FL-21: route ETA uses OSRM r['duration']/60 with 30km/h estimate fallback (nearby_map_screen _getDirections).
- FL-22: ReportModel.latitude/longitude now nullable (null when missing/0); consumers guarded (report map screen placeholder, details/card texts show 'No location data', directions guarded w/ snackbar); offline_db cachePlace/cachePlacesBulk skip missing/(0,0) coords.
- FL-27: my-location button toggles follow mode on/off (2nd tap stops); stream already followed when flag set.
- FL-28: context.read removed from dispose() in reports_list_screen, profile_screen, place_details_screen — providers captured in initState (late final fields).
- FL-29: home_screen _onTabChanged stops ReportProvider auto-refresh when leaving reports tab, restarts + refetches when entering.
- FL-30: VERIFIED not a bug — recently_viewed has PRIMARY KEY (place_id), dupes impossible, NOT-IN prune keeps exactly 50 rows.
- FL-31: add_place omits empty phone/website keys (both online + offline sync paths).
- FL-32: ad_provider banner ads no longer duplicated into reportAds; partners district param VERIFIED already wired (IN-01 rework); subscription plan id parsed via num/tryParse (was `as int?` crash); camera gallery disable is intentional-by-design (anti-fake-report layer 1, documented) — no change; alerts_screen poll notifications now use persistent seen-keys set (seeded on init) — no repeat snackbars; push_notification_service navigator key now set in main() + passed to MaterialApp (was never called).
- flutter analyze project-wide: 0 errors (354 pre-existing warnings/infos).
## Phase 4 (Cleanup) - item 34: Backend BE-06/09/12/18/20/22/23 - DONE

- BE-06: LeaderboardController alerts join lerts.user_id -> lerts.created_by (Alert relation is hasMany(created_by); old column caused SQL error on rank+list pages).
- BE-09: equireAdmin (role + route-name permission, pattern copied from AchievementController) added to AiAgentController (index/store/update/destroy/run) and AiAgentTaskController (index/store/retry) - they previously had ZERO auth; routes web.php:150-156 only carry ['auth','status'] middleware.
- BE-12: AdController::trackImpression now rejects inactive/expired/budget-exhausted campaigns (422) and dedupes impressions (one per user per campaign per 10 min).
- BE-18: PlaceController OSM-auto place creation now is_active => false (was true - bypassed moderation). show()/reviews still work for inactive places, only listing hides them.
- BE-20: AdCampaignController::requireAdmin gained the route-name Permission check (had role check only).
- BE-22: ReportController::show now 404s non-approved reports unless requester is owner or admin/moderator.
- BE-23: AlertController emergency feed now only approved reports (was approved+pending).
- All 8 touched files php -l clean. Plan item 34 -> [x].
## Phase 4 (Cleanup) - item 35: BE-24->BE-27, BE-17 - DONE

- BE-17 (nit): admin booking discount now gated on discount_type && discount_value exactly like ShopService::applyToBooking (was applying value_npr when type empty -> inconsistent with API path). State check (is_used/consumed_at/booking_id) was already done in Phase 2 item 16.
- BE-24: VERIFIED already fixed - ReportManagerHandler.php:65 isset guard + :76 ?? 'not-set'. No change.
- BE-25: AdminController live map (:1218) + CustomerSupportHandler::getLiveAlerts (:151) now use whereNull('expires_at')->orWhere('expires_at','>',now()) - permanent alerts (NULL expiry) were silently dropped from admin map / AI context (also fixed >= vs > boundary).
- BE-26: ModeratorService::logSecurity rewritten - writes the audit row FIRST, then counts (includes current attempt), updates entry metadata if threshold (5 failed logins/15min, 10 unauthorized/1h, 10 permission denials/1h) hit. Old code counted before insert -> threshold effectively 11 and current attempt never counted.
- BE-27: retired BOTH AssignFreeSubscription + AssignFreeSubscriptions commands (moved to temp retired-commands backup) - User::created() hook (User.php:117-128) auto-creates active free subscription, so both commands were permanent no-ops with conflicting semantics. No other references to assign-free signatures.
- All 4 touched files php -l clean. Plan item 35 -> [x].
## Phase 4 (Cleanup) - item 36: IN-05, IN-09->IN-14 - DONE - ALL 73 ISSUES COMPLETE

- IN-05: VERIFIED already fixed - AuthController::update validates expertise_regions (array), in  + cast. No change.
- IN-09: PlaceController::show for unreviewed osm_ ids now returns 200 {success:true, data:null} instead of 404 (client guards null; reviews endpoint already returns [] for unreviewed).
- IN-10: removed dead getUserReputation (GET /users/{id}/reputation - route never existed, zero callers).
- IN-11: ProfileStats.fromJson xpBreakdown now  is num ? v.toInt() : 0 (was (v as num).toInt() crash on nested rates map).
- IN-12: ProfileController alerts_by_severity 'low' -> 'info' (enum is info/medium/high/critical; 'low' never exists).
- IN-13: verification_tick aligned - backend maps none->gray at 4 send sites (ProfileController, UserProfileController, LeaderboardController x2); Flutter defaults changed 'none'->'gray' (user.dart ctor, profile_provider parse+ctor, leaderboard_provider parse+ctor, user_public_profile_screen) - no more NONE TICK, unverified users show GRAY TICK. NOTE: edited via PowerShell -> BOM got added -> stripped BOM from 3 PHP files (read bytes 0-2 = EF BB BF).
- IN-14: removed dead createAlert + getNearbyAlerts + getWeatherGrid from api_client (zero callers; map screen uses dio directly; IN-04 logout issue already fixed in Phase 1).
- flutter analyze: 0 errors (354 pre-existing warnings/infos). php -l clean on all 4 touched backend files.
- LOGIC_AUDIT_FIX_PLAN.md: ALL 73 items now [x].

## Nearby screen fixes (user reported)
- Fixed locate-me button on both NearbyMapScreen & NearbyPlacesScreen: always recenters to exact GPS location; fetches fresh location if unknown; shows error snackbar on failure (was: first tap did nothing when tracking was on, or silently failed).
- Added always-visible 'You are here' marker (blue dot + label) on NearbyMapScreen regardless of places toggle.
- Added 'Try Again' button on the waiting-for-location card (both screens).
- Fixed FilterPlacesSheet chip contrast: selected chip now solid primary bg + white text/checkmark (was primary 0.2 bg + primary text = same hue). Added initialFilter param so sheet state persists.
- Fixed verified/featured filters on NearbyPlacesScreen: onApply was ignoring filters entirely; now stores PlaceFilter, applies client-side to list+markers, passes categoryId/radius to fetch.
- Fixed bbox cache invalidation on NearbyMapScreen filter apply so refetch uses new filter.

## Google Maps -> OpenStreetMap full conversion (user: "kunai pani map maa google map halni haina, OSM matra")
- nearby_places_screen.dart fully converted from google_maps_flutter to flutter_map 7.0.2:
  - _buildMarkers now emits flutter_map Marker widgets (colored circle + category icon via _getCategoryIcon, selected = 40px + white border + glow, tap -> _onPlaceTap). REMOVED Google Marker infoWindow + _colorToHue helper.
  - _buildYouAreHereMarker return type Marker (not Widget), added to its own MarkerLayer above place markers.
  - Camera moves: _mapController.move(LatLng, zoom), fitCamera(CameraFit.bounds(bounds:, padding:)), zoom FABs use _mapController.camera.zoom +/- 1 clamped 5..20.
  - _latLngBoundsFromPoints returns flutter_map LatLngBounds(corner1, corner2) - FACTORY not const (empty-case const removed).
  - Compass overlay + _buildCompass() + _resetRotationToNorth() (rotate(0)); needed import 'dart:math' as math.
  - onMapEvent -> _currentZoom write (field otherwise unused, pre-existing warning).
- Removed google_maps_flutter from pubspec.yaml (was ^2.12.0, zero code references left). No Google API keys in AndroidManifest/Info.plist - clean.
- flutter analyze: 0 errors. flutter build apk --debug: BUILD SUCCESS.
- External Google Maps links (url_launcher directions in _getDirections / place details "View on map") intentionally KEPT - they open the OS map app, not in-app Google embeds.
