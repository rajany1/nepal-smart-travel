Objective (updated):
- Make every Settings function genuinely workable (app + backend) — completed and verified.
- Implement real English ↔ Nepali language switching for all app UI texts, plus an admin‑site "Translator" section where admin/clients can add/manage translation words — completed across all screens + backend.

Important Details:
- Booking system: user chose "App UI matra" scope — all booking UI removed from Flutter; backend routes/tables/controllers remain fully intact for future re‑enable.
- Language = en/ne persist only (dictionary‑driven, no full l10n framework).
- Data Saver / Auto‑download = save tiles to device cache so the map works offline without data (already wired).
- MariaDB rejects CAST('false' AS JSON) — use JSON_EXTRACT(settings, '$.show_on_map') = 'false'.

Completed (this session + prior):
- Backend: TranslationController (GET /v1/translations dictionary), TranslatorController (CRUD + bulk import + search, routes/web.php admin group + layout.blade.php sidebar link), AppUiWordSeeder (~180 UI words + 15 new settings‑screen strings, registered in DatabaseSeeder), translation_glossary table populated.
- Flutter: LocalizationService (ChangeNotifier, SharedPreferences app_language, t(key) fallback=key, setLanguage), ApiClient.getTranslations(), main.dart wiring (Consumer<LocalizationService> + ValueKey(language) rebuild), settings_screen.dart live‑language dropdown, all hardcoded strings → context.t('...') in 4 parallel batches (auth+profile, home+places, reports+emergency+alerts, offers+store+subs+assistant).
- All 4 batches: 0 new dart analyze errors; pre‑existing ~350 info/warnings unchanged.

Files touched:
- Backend: app/Http/Controllers/Api/TranslationController.php, app/Http/Controllers/Admin/TranslatorController.php, routes/api.php, routes/web.php, resources/views/admin/translator.blade.php, resources/views/admin/layout.blade.php (sidebar link), database/seeders/AppUiWordSeeder.php, database/seeders/DatabaseSeeder.php (registration), database/migrations/... (glossary already existed).
- Flutter: lib/core/services/localization_service.dart, lib/core/api/api_client.dart (getTranslations()), lib/main.dart (provider + MaterialApp key), lib/features/profile/settings_screen.dart (full rewrite with i18n), plus 31 screen files across 4 batches with context.t() wraps.

Work State:
- All 4 batches complete — 0 new dart analyze errors.
- Backend verified via php -l + tinker dictionary test (Settings→सेटिङ्स, Map→नक्सा).
- No further action needed; the app now supports live EN↔NE UI switching via the admin Translator page and per‑user language preference.