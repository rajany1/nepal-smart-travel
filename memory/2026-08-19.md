# 2026-08-19 — Admin delta-sync + Explore/Map UI polish

## Admin realtime delta-sync (backend) — DONE + verified
- LiveFeed class, LiveFeedController + 3 routes, delete bumps (14 methods), stats endpoint (flat keys incl. chart_today/peak/avg, xp_*), 21 list blades wrapped in `#liveTable`, dashboard `data-count` spans, layout poller.
- **Root cause of "still refreshing": Hotwired Turbo** did full body-replace on every click/form. Removed Turbo entirely → native nav + surgical AJAX interceptors (capture-phase click on `#liveTable` same-path links, bubble-phase submit skip modal forms, `fragment=1`, `applyParsed`, replaceState, body `data-since` copy).
- Layout IIFE got accidentally broken during an edit (submit listener body deleted) — fixed, verified `node --check` (JS SYNTAX OK), `php artisan view:cache` OK.
- Backend e2e verified (insert→new, update→updated, delete→deleted, delsSeen filter). HTTP smoke 302→/login OK. User to hard-refresh (Ctrl+Shift+R) + 2-tab test pending.

## Flutter Explore screen polish — DONE
- New `widgets/explore_place_card.dart` (strip + expanded variants; cached images, category icon/color fallback, FEATURED ribbon, verified, district·distance, rating).
- New `features/places/explore_search_screen.dart` — client-side search over `nepalPlaces` (Redis-cached /places/all, 1000 cap) — avoids live Overpass hammering; 300ms debounce.
- `home_screen.dart` `_ExploreTab` rebuilt: search hero, welcome card w/ district, Discover category chips (9, name contains-match — DB categories messy: id13=Education 23.7k, Restaurants aliases), Quick Actions 6/3-col, polished Nearby Highlights (tappable → PlaceDetailsScreen), Featured strip (fetchFeaturedPlaces), alerts with time+district meta, `_AlertCard` updated, old `_PlaceCard` removed, unused imports cleaned.

## Duplicate screen removed — DONE
- `nearby_places_screen.dart` (2196 lines) DELETED — was a twin of `nearby_map_screen.dart` (~3300 lines).
- Destination support ported into `NearbyMapScreen`: `destinationLat/Lng/Name` params, auto `_fetchDestinationRoute()` in `_initMap`, pink flag marker, `_buildDestinationPreview()` card (Go/Clear) in sheet.
- Call sites: `place_details_screen._openDirections` + `assistant_screen` nearby action → `NearbyMapScreen`.

## Nearby screen UI declutter + polish — DONE
- FAB stack 9 → 4: removed zoom+/−, ⭐featured, ☁️weather, 🥾hiking FABs. Kept my-location, filter, add-place. (⭐ already covered by filter sheet "Featured only"; ☁️/🥾 moved to filter sheet.)
- Filter sheet: added "Map Layers" section (Show weather overlay / Show trekking routes via MapViewProvider) + SingleChildScrollView wrap. `_onFilterTap` `.then` → `_loadRouteOverlays()` if enabled.
- Sort chips in sheet header: Nearest / Rating / Featured (`_sortMode` + `_sortedPlaces`).
- List item polish: 60px thumb radius 12, FEATURED gold ribbon overlay, colored category chip, district meta, review count `(n)`, kept OSM badge + compact save icon.
- `_showFeaturedOnly` state removed → uses `_activeFilter?.onlyFeatured`.
- `flutter analyze`: 0 errors (pre-existing warnings remain). APK built: `mobile_app/build/app/outputs/flutter-apk/app-debug.apk`.

## Localization note
- New UI strings used via `context.t(...)` (Nearest/Rating/Featured/Destination/Go/Clear/Tap Get Directions for route) — fallback shows key if missing from strings file. Add translations if strict.

## Reports screen polish — DONE
- Header: "Reports" title + green Live badge (pulsing) + segmented pill toggle (Recent green / Emergency red, TabController `_onTabChanged`).
- Emergency cards: left 4px accent bar + tint bg + ⚡EMERGENCY ribbon; category color chips via `_getReportCategoryColor(icon)` switch (road→0xFFF39C12, warning→error, ac_unit→0xFF5C6BC0, directions_bus→0xFF8E44AD, explore→primary, local_gas_station→0xFF16A085, event→0xFFE91E63, info→infoColor, default→primary); raw coords → "View map" chip.
- Details sheet: drag handle + location tile (district · View on map →).
- Submit sheet: GPS status chip (Getting location…/district/Location detected/GPS needed), 110h dashed photo capture area, 50h bold rounded submit button.
- Pre-existing warnings left (unused app_constants import, unnecessary !, isHighPriority/statusIcon unused, invalid_null_aware_operator:1504).

## Emergency screen rewrite — DONE
- `emergency_screen.dart` fully rewritten: pulsing `_SOSPulseButton` (AnimationController 1100ms repeat reverse, scale+glow, red gradient), gradient quick-dial 2x2 tiles (Ambulance 102 / Police 100 / Fire 101 / Hospital→ExploreSearchScreen — dead button FIXED), tappable service cards (Blood Bank/24-7 Pharmacy/Mountain Rescue→ExploreSearchScreen, Tourist Police→tel:1144), `_SectionHeader`/`_ServiceCard`/`_TipsBox` (Dart 3 records), polished SOS dialog. analyze 0 issues.

## Profile screen polish — DONE
- AppBar → primaryColor bg + white fg (matches gradient header).
- Verified tick moved bottom-right → bottom-LEFT (was overlapping edit button).
- Header chips + stats labels + XP card + sections + more-menu items localized via `context.t(...)`.
- Store button outlined → gradient filled (secondaryColor, r14).
- Verification section: `verificationTick == 'none'` → "Get Verified" encouragement card (workspace_premium_outlined) instead of NONE TICK card.
- Details sheet: raw coords → `report.district`.
- Empty reports state: added "Go to Reports" OutlinedButton → `ReportsListScreen` (new import).
- Removed unused `user.dart` import. analyze 0 errors, APK built.

## Edit Profile screen polish — DONE
- **BUG fixed**: avatar pick was dead (local path never uploaded). Now picked image → base64 → `ProfileProvider.updateAvatar()` (`POST /profile/avatar`, backend accepts Base64 or URL). Preview shows local file immediately; camera chip shows spinner while picking.
- Fields grouped into SectionCards by type: Basic Info (text/email/phone/select/number/date/checkbox), About (textarea), Preferences (multiselect) — works with any backend schema.
- Inputs (dynamic_profile_field.dart): radius 8→12, white fill, shared `_baseDecoration` for all field types, focusedErrorBorder added, date field suffix icon themed + initialDate from current value (was DateTime.now()).
- Save button: gradient (primary→primaryLight) + shadow, h50 r14 bold.
- Loading spinner primary-colored; all strings localized via `context.t()`; unused localization import + unreachable switch default cleaned.

## profile_setup_screen.dart avatar fix — DONE
- Same dead avatar pattern fixed: picked image → base64 → `ProfileProvider.updateAvatar()` on save; local preview via `Image.file`; `_avatarChanged` flag; unused localization import removed. analyze 0, APK built.

## Edit Profile screen "not workable" — root-cause fixes — DONE
- **Root causes fixed:**
  1. Avatar base64 data URI stored raw in DB → `NetworkImage` CANNOT load data URIs → avatar broke everywhere. Backend `ProfileController@updateAvatar` now decodes base64 → saves to `backend/public/uploads/avatars/avatar_{id}_{time}.jpg` → returns real URL (`url()` helper). Old raw-string fallback kept.
  2. Double PUT on save: `api.updateProfile` + `AuthProvider.updateProfile` hit `PUT /users/me` twice → removed the redundant direct call (single `AuthProvider.updateProfile`).
  3. Avatar upload failure aborted whole save → now tolerant (continues, warns in snackbar).
  4. Empty form when `ProfileProvider.profile == null` → fallback to `AuthProvider.user` fields (UserModel has no gender/interest → those null).
  5. Dropdown crash risk: `DropdownButtonFormField` asserts if stored gender/interest value not in options → `_buildSelectField` now guards (value only set when present in options).
- `php -l` OK, analyze 0, APK built.

## Pending
- User: hard-refresh admin + 2-tab delta test; install new APK and eyeball Explore + Nearby screens.