# 2026-08-28 · ORIPORI branding + mp4 video splash + native splash

User gaved 4 files (oripori_logo svg x2 + mp4 videos) and said: app is shown externally as
**ORIPORI**; change native splash + splash logo to ORIPORI, add the ~3s mp4 video splash, fast load.

**Done:**
1. Copied videos → `mobile_app/assets/videos/oripori_logo_light.mp4` (from Desktop/oripori_logo.mp4),
   `oripori_logo_dark.mp4` (from Desktop/oripori_logo_dark.mp4). Added `assets/videos/` to pubspec assets.
2. Added `video_player: ^2.9.2` to pubspec (got dependencies OK).
3. `splash_screen.dart` rewritten — plays brand mp4 (dark theme → oripori_logo_light.mp4, light →
   oripori_logo_dark.mp4 to match pre-existing logo mapping), waits for video before routing (capped 6s,
   never blocks on stuck video), keeps guest-mode logic, `AnchorLogoSkeleton` placeholder while init.
   Analyzer clean.
4. Native splash regenerated via `flutter_native_splash:create` with ORIPORI logos + dark variant
   (color #031a38, dark #0d1117) → Android 12 + night + iOS LaunchScreen all updated. Config updated
   in pubspec (flutter_native_splash block: added color_dark/image_dark/android_12 dark).
5. Android label `nepal_smart_travel` → `ORIPORI` (AndroidManifest).
6. `AppConstants.appName` → 'ORIPORI', tagline → 'Smart Travel Intelligence' (removed unused
   localization_service import).

**Verified:** `flutter build apk --debug` SUCCEEDED (app-debug.apk 276MB debug). No analyzer issues.

**Notes:** native splash is a static image (Android 12 windowSplash is image-based, can't play video);
the mp4 plays as the Flutter splash right after. APK is debug (large, all ABIs); release build smaller.
iOS LaunchScreen images generated but not build-verified (Windows host).
# 2026-08-28 · Guest-mode architecture (no login wall)

User gave an architecture diagram: app opens → HOME/MAP → GUEST MODE (explore free
without login) → Explore/Nearby/Emergency all ✅, but any ACTION (Report/Save/Post)
shows "login required" → Login/Register.

**Backend:** no change (APIs already allow anonymous reads; write endpoints already
auth-guarded).

**Mobile changes:**
1. `splash_screen.dart` — `_init()` now ALWAYS routes to `/home` (guest or logged in).
   No login wall. Auth restore runs in background (non-blocking); logged-in users warm
   profile settings + still get profile-completion redirect. Guests skip straight home.
2. NEW `core/services/auth_guard.dart` — `requireLogin(context)` helper: if
   `AuthProvider.isAuthenticated` false → show "Login Required" dialog → on confirm
   `Navigator.pushNamed('/login')`.
3. `reports_list_screen.dart` — `_showSubmitReportSheet` gated with requireLogin
   (Report action). `_submitComment` gated (Post action).
4. `home_screen.dart` — "New Report" quick action gated with requireLogin;
   `_currentIndex` default 0 = **Explore** tab (was 2=Reports), so guests land on free
   browsing not the action-heavy Reports tab.
5. Save flow (`nearby_map _confirmSaveOsmPlace`) + offer claim were ALREADY login-gated.

**Verified:** flutter analyze 0 errors (75 pre-existing info/warnings). Profile tab
handles guest gracefully (no crash). `/login` route exists.

**Gotchas:** home uses IndexedStack (builds all tabs incl Profile) — ProfileScreen
already guards on isAuthenticated so no guest crash. Reports LIST is browsable by guest;
only SUBMIT/comment require login.

User asked:
1. Admin/moderator le already-approved gareko report AI le naheros.
2. Admin alert message aauthena — lat/lng add hudaina; map bata location select garne
   ya sabai users lai broadcast garna milos.

**#1 AI skip human-approved:**
- `ReportAnalysisService::process()` now guards `verified_by !== null || status !== 'pending'`
  → skips. Human decision final.
- `AnalyzeReport::handle()` same guard `verified_by !== null || status !== 'pending'` → return.
- So admin-approved/rejected reports never AI-reviewed (even if queued earlier).

**#2 Alert location/broadcast:**
- Migration `2026_08_28_000001_add_is_broadcast_to_alerts_table` → `alerts.is_broadcast` bool.
- `Alert` model fillable + cast boolean.
- `AdminController::createAlert` accepts nullable lat/lng + is_broadcast; if broadcast → clears
  lat/lng, dispatchForAlert routes to broadcast.
- `alerts.blade.php` form: added Leaflet map location picker (click → hidden lat/lng + marker)
  + "Broadcast to ALL users" checkbox (hides map when checked).
- `PushNotificationService::notifyAllUsers()` new — all subscribed tokens, no location scope.
- `SendAllUsersPushNotification` job + `AlertPublisherService::dispatchAll()`.
- `AlertController::index` + `nearby()` now include broadcast alerts regardless of location.

**Verified:** is_broadcast col YES; admin alerts page 200 (map+broadcast present); AI on
human-approved → SKIP (correct). php -l all clean.

**Gotchas:** alert push uses settingsKey 'notifications_enabled' (not 'push_notifications'); broadcast
alerts have null lat/lng so mobile must render location-less alert fine; Leaflet loaded via unpkg CDN
in alerts.blade (works with SPA layout — script injected on swap).

User chaato thiyo: "Mobile report screen ma user le only description ra photo submit garna milni,
system le title/category/priority decide garos (system architecture bata)."

**Root cause of UX:** Previous app let user pick category + priority + title manually (via dynamic
form-config fields), which is friction + requires domain knowledge.

**Design — rule-based, no AI dependency (instant & reliable):**
1. NEW `backend/app/Services/ReportAutoClassifyService.php` — deterministic:
   - `classify(description)` → {title, category_id, category_name, priority}
   - `pickCategory()` keyword match against 8 seeded report_categories
   - `pickPriority()` severity keywords → critical > high > medium
   - `makeTitle()` first-sentence, title-case, ~90 chars
2. `ReportController::store()` — title/category_id/priority now OPTIONAL in validation;
   when missing, `ReportAutoClassifyService` fills them (merged into request before validate).
   validated array copies from $request->input so auto-inferred values persist.
3. `ReportController::formConfig()` — fields now ONLY `description` (textarea, rows 5, min:10).
4. Mobile `report_provider.dart::submitReport` — title/categoryId/priority params optional;
   FormData conditionally includes them.
5. Mobile `reports_list_screen.dart::_submitReport` — sends desc/lat/lng/district/photo/GPS only.

**Verified:** auto-classify (landslide→Road&Traffic/critical, flood→Safety/critical, water
outage→Services/high, jatra→Events/medium, potholes→Road/high, hidden waterfall→Hidden/medium);
form-config desc-only 200; flutter analyze 0 errors (78 pre-existing); php -l clean.

**Gotchas:** report_categories seed IDs assumed (1..8); `DynamicFormField` supports textarea+rows;
photo + is_live_capture + lat/lng + EXIF GPS flow unchanged.


## BUGFIX (same day): "Target class [App\Http\Controllers\ReportAutoClassifyService] does not exist"
- Report store le `app(ReportAutoClassifyService::class)` call garthe but controller ma
  `use App\Services\ReportAutoClassifyService;` import chaina thiyo → `App\Http\Controllers\...`
  namespace bhitra resolve hune → "does not exist".
- Fix: `backend/app/Http/Controllers/ReportController.php` top ma import add gare.
- Verified: `app(\App\Services\ReportAutoClassifyService::class)` resolves, classify OK.


**Problem:** App ka splash (cold start) kam 1 min samma load hune, UX kharsi.
**Root cause:** `mobile_app/lib/main.dart` le `runApp()` aagi (`await`) garthe:
- `authProvider.initializeAuth()` — session restore + network
- `localizationService.init()` → `getTranslations()` (network)
- `pushService.initialize()` → native `requestPermission()` dialog + `getToken()` (network)

Sabi `await` pura na huee native (platform) splash frozen — permission dialog / slow link le
1 min+ hang.

**Fix:**
1. `main.dart`: `runApp()` ab turunta (Firebase init pachi matra), ra heavy bootstrap
   `unawaited(pushService.initialize())` + `unawaited(localizationService.init())` background
   ma defer. `await authProvider.initializeAuth()` main bata hatyae — flush splash
   (`AuthInitializationWrapper._init`) le auth routing own garchha.
2. `splash_screen.dart`: `_init` ma hard 12s `.timeout()` (network stall bhaye pani splash
   kabhi hang hudaina — login/home ma fallback), ra branded splash (travel_explore icon +
   appName + tagline) banaye. Har `await` pachi `if (!mounted)` guard.
3. main.dart ko duplicate `import localization_service.dart` hatyo (duplicate_import).

**Gotcha note:** AuthProvider `_isInitialized` guard — splash le initializeAuth call garchha,
main bata hoina. PushNotificationService singleton factory, `initialize()` idempotent.
`flutter analyze` duita file: "No issues found!" (pura project 0 errors, ~446 pre-existing
warn/info — unchanged).

**Related (previous session admin OSM/district import):** DB ma OSM import background ma
chalya (77 districts queue). `backend/osm_state_check.php` (temp, cleanup note: rackha thio,
hataidiyo pachi manually clean).