# 2026-08-09 — Monetization Rework: Sponsors/Store Out, Offers+Ads In

## Objective
Replace the old sponsors + XP-store monetization with a unified offers-based model: XP-priced reward offers (claimable), offer codes applied to bookings for discounts, and context-aware ad campaigns with impression/click tracking. Remove all sponsors & store code from both stacks.

## Backend (all verified)
- **Sponsors & store API removed**: `SponsorController`, `StoreController`/store-item & store-purchase routes/controllers gone; legacy store tables dropped via `2026_08_08_170200_drop_legacy_store_tables`.
- **Offers re-priced with XP**: `reward_offers.price_xp` added (`2026_08_08_170000`); `Api\OfferController::publicOffer()` returns `price_xp`; claim = deduct XP atomically (same "claim deducts, cancel refunds" semantics as before — but booking-cancel/release path intentionally does NOT refund XP).
- **Bookings ↔ offers**: `POST /api/v1/bookings/{id}/apply-offer` matches redemptions by `status='claimed' && booking_id null && consumed_at null` (migration `2026_08_08_170100_add_booking_tracking_to_offer_redemptions_table` adds `applied_at/consumed_at/booking_id`); discount from `offer_type` percentage_off/fixed_off (clamped, never below zero); release on cancel. Routes: `applyOfferToBooking`, `consumeRedemption` (already existed on bookings routes).
- **Ad campaigns**: `ad_campaigns` gains `targeting` JSON (`adContext` home/report/nearby, districts, categories) via `2026_08_08_170300_add_targeting_to_ad_campaigns_table`; `ad_clicks` table `2026_08_08_170400`; settings `2026_08_08_170500_add_ad_and_offer_settings` (default_offer_expiry_days, ad_impression_dedupe_minutes). `AdController`: public `active` (context+district+category filter, geo radius option), `trackImpression` (dedupe per user per campaign within minutes-window), `trackClick` (inserts ad_clicks row).
- Admin: offers index/approve/pause/reject/resume already existed; ad campaigns CRUD + pause/resume added in admin (routes confirmed via `route:list`).
- Verified: `php artisan migrate:status` all ran (through 170500); `php artisan view:cache` PASSES; `route:list` shows all offers/ads routes; live smoke `GET /api/v1/ads/active` → 200 `{"data":[]}` (no active campaigns — expected).

## Flutter (`mobile_app/`)
- **StoreScreen rewritten** as all-in-one 3-tab (`IndexedStack`): **Rewards** (`StoreXpHeader` + `ExploreOffersView`), **My Codes** (`MyCodesView`), **Bookings** (`MyBookingsScreen(inTab: true)` — new `inTab` param hides AppBar when embedded). Old shop/orders tabs & StoreProvider gone.
- **offers_screen.dart**: made `ExploreOffersView`/`MyCodesView`/`CodeCard` public (reusable in StoreScreen); `OfferCard` shows gold "N XP" chip when `offer.priceXp > 0`.
- **offer_model.dart**: `OfferModel.priceXp` (parses `price_xp`); `OfferRedemptionModel` + `appliedAt/consumedAt/bookingId` + `isAvailable` getter (`status=='claimed' && bookingId==null && consumedAt==null`). Removed unused `foundation.dart` import.
- **booking_form_screen.dart**: shop codes → **offer codes**: `_selectedCode` String + `_selectedCodeData`; discount from `code['offer']` offer_type (clamped); submit sends `offer_code` (was `shop_code_id`); UI section "Available Offer Codes" with Apply/Remove keyed on code string. Fixed leftover `_selectedCodeId` (undefined var, caught by analyze).
- **api_client.dart**: removed `getStoreItems/purchaseItem/getMyPurchases/getSponsors`; `getAvailableCodes`→`getAvailableOfferCodes()` (GET `/offers/available`, parses `response.data['redemptions']`); `getActiveAds({adContext, district, category, limit})`; new `trackAdImpression`/`trackAdClick` (POST).
- **ad_provider.dart**: `fetchActiveAds({adContext, district, category, limit})` + fire-and-forget `trackImpression(ad)`/`trackClick(ad)`.
- **ad_banner_carousel.dart**: `AdBannerCarousel({adContext = 'home'})`, impressions fired per ad on load, `_onTap` tracks click + opens `target_url` (url_launcher).
- **ad_cards.dart**: `AdPlaceCard`/`AdReportCard` `_onTap` now calls `ApiClient.instance.trackAdClick(ad.id)` before launching URL.
- **Context ads wired**: home carousel (default 'home'), `reports_list_screen.dart` → `adContext: 'report'`, `nearby_places_screen.dart` → `adContext: 'nearby'`.
- **Sponsors removed everywhere**: home_screen (section + `_SponsorsLogoStrip` deleted, file now 579 lines), main.dart (`/sponsors` route), profile_screen (ListTile), `sponsors_screen.dart` deleted, empty `features/sponsors` dir removed.
- **Dead store files deleted**: `providers/store_provider.dart`, `store_item_card.dart`, `store_order_card.dart`, `store_category_tabs.dart` (kept `store_xp_header.dart`); removed StoreProvider registration in main.dart.
- **Analyze**: 0 errors; remaining warnings ALL pre-existing (verified baseline list). Cleaned 2 unused imports I'd touched (offer_model foundation, store_screen app_theme).

## Gotchas
- **Workdir typos are the #1 time-waster**: correct path `C:\Users\ACER\Desktop\nepalsmarttravelandlocalintelligenceplatform` — no hyphens, "trave**land**" not "trave-land". I repeatedly typed `trave-land` → `NotFound` on edit/bash. Use `(Get-ChildItem C:\Users\ACER\Desktop -Directory | ? Name -like '*nepal*').FullName` to resolve dynamically instead of typing it.
- PowerShell `.Replace()` with literal `\r\n` won't match file's actual line endings — use `Get-Content` + `Where-Object` filter + `Set-Content` (no BOM issues since it's Dart, not PHP).
- `php artisan analyze` / `php artisan memory` don't exist (no such commands) — don't re-run.
- Laravel dev server was already running on :8000 during smoke test — nice for live checks.

## Status
Backend fully done + verified (migrate, view:cache, routes, live smoke 200). Flutter done + analyze clean. NOT yet tested on device: live claim→apply-offer→discount flow, ad impression dedupe across sessions, XP refund on cancel (intentional no-refund on booking release), StoreScreen tab UX.

## Update (same day) - Partner-Pays Ads + Offer Commission + eSewa/Khalti
- **New business model**: partners PAY prepaid ad budgets (admin earns the spend, auto-pause at budget exhaustion); partners EARN from offers = offer value - admin commission % (default 10, setting offer_commission_percent). Gateways eSewa+Khalti (sandbox) for partner ad payments only.
- **Migration** 2026_08_09_100000_add_payments_and_commission_fields (ran): ad_campaigns + paid_amount/spent_amount/payment_status/gateway/gateway_ref; ad_payments table (gateway, amount, status, transaction_id, unique reference, metadata json, paid_at); reward_offers + value_npr; offer_redemptions + value_npr/commission_percent/admin_commission/partner_earnings; seeds offer_commission_percent=10.
- **PaymentGatewayService** (new): eSewaForm/verifyESewa (HMAC-SHA256 base64; sandbox EPAYTEST / 8gBm/:&EnhH.1/q / rc-epay.esewa.com.np), initiateKhalti/verifyKhalti (dev.khalti.com, paisa x100, pidx). config/payments.php + env keys appended to .env & .env.example (KHALTI_SECRET_KEY empty - khalti flow 401s until test key provided).
- **Partner flow**: routes partner.ads.pay (GET/POST initiate), partner.payments.esewa.callback + khalti.callback; pay page partner/ad_pay.blade.php + auto-submit gateway_redirect.blade.php; callbacks verify then mark paid (reference format adpay-{id}-{STR6}).
- **Spend**: AdCampaign::calculateSpend() = impressions/1000*ad_cpm + clicks*ad_cpc (50/10 defaults); API AdController applySpend() updates spent_amount and auto-pauses at budget; hasBudget() now checks spend.
- **Admin**: approve() blocks unpaid partner campaigns; new refund() action (payments->refunded, campaign rejected, logs ad-campaign.refunded); index stats revenue + unpaid; admin/ad_campaigns.blade.php updated (revenue card, Budget/Paid + Payment columns, awaiting-payment note, refund button); admin/settings.blade.php + offer_commission_percent field.
- **Partner blades**: dashboard (Paid/Spent on ads + Earned from offers cards - removed ads_total/ads_active), ads (Budget/Spent/Remaining, pay button, payment badge), ad_form (Budget field), offers (Value/Earned columns), offer_form (value_npr), redemptions (Value/Commission/Your Earnings columns).
- **UTF-8 corruption lesson**: my Get-Content (ANSI default) + WriteAllLines round-trips double-encoded multibyte chars (emoji, middle dots, em-dashes) into A??'A,A mojibake in dashboard/ads/ad_form blades. Fixed by reading with [System.IO.File]::ReadAllLines(C:\Users\ACER\Desktop\nepalsmarttravelandlocalintelligenceplatform\memory\2026-08-09.md, UTF8Encoding) and replacing lines with ASCII. ALWAYS use -Encoding UTF8 / explicit UTF8Encoding + read tool, never bare Get-Content+Set-Content on non-ASCII files.
- **Verified**: php -l all 14 touched PHP files clean; view:cache passes; route:list shows all 5 new routes; migrate:status ran; tinker smoke: eSewa form renders with signature, config values (min_budget=100, commission=10, EPAYTEST) OK; server on :8000 still up.
- **Not yet tested**: live sandbox payment round-trip (needs browser + partner login), khalti (needs secret key), real spend accrual over time.

## Ads system made proper (Flutter + backend)
- Backend: /api/v1/ads/track-impression + track-click moved OUT of auth group (public now), throttled 60/min, guest dedupe now by IP (was global 10min - fixed), paid gate in active() (payment_status=paid OR budget<=0), Admin refund method added (was 500).
- Flutter: AdPlaceCard/AdReportCard -> StatefulWidget + impression tracking in initState; new lib/widgets/ad_inline_banner.dart (AdInlineBanner self-contained compact ad + adContextForCategory/adCategorySlugForCategory helpers); placed on place_detail screen (with category+district targeting), explore context in map bottom sheet header (nearby_map_screen.dart), category-aware ad refetch on nearby_places_screen category chip select (hotels/restaurants/cafes/attractions/activities contexts).
- Verified: php -l clean, routes registered, live HTTP tests (active 200, impression/click 200 public, dedupe 422, unpaid NOT served / paid served), flutter analyze 0 errors 0 warnings (368 pre-existing infos).

## Ads business-logic hardening (system decides, not partner)
- active() abha budget gate pani check garcha: budget>0 huda spent_amount < budget matra serve (budget khatam ko ad khulla dekhi free views hudaina). Plus existing paid gate + max_impressions gate.
- Admin approve()/resume() abha cannotRunReason() guard: payment pending / budget exhausted / campaign ended => block with clear error.
- Partner resume() pani guarded (pending resubmit matra, budget/time/payment check), message: 'Pay the campaign budget first - free campaigns are created by the admin only.'
- Partner budget validation abha 'required|numeric|min:100' (pehla nullable min:0 => partner free campaign loophole); store() ko ?? 0 default hataiyo; ad_form blade budget input min=100, default 100.
- Tests: exhausted NO serve / healthy YES / free(admin, budget 0) YES / unpaid-with-budget NO; cannotRunReason: exhausted/ended/unpaid messages verified; php -l + view:cache clean.

## Pause/resume UX fixed (user: 'resume nai vayena' + admin confusion)
- Root: partner resume was setting status='pending' (admin re-approval needed) -> user expected instant resume; and pause reason was never shown; admin vs partner pause indistinguishable.
- Migration 2026_08_09_150000_add_paused_by_to_ad_campaigns_table: paused_by column (admin/partner/system).
- API applySpend auto-pause -> paused_by='system'. Admin pause->'admin', admin resume/approve->active + paused_by=null. Partner pause->'partner', partner resume -> INSTANT 'active' (guards: admin-locked block, paid block, budget block, ended block).
- Partner blade: pause reason shown (Budget exhausted / Paused by admin / Ended / Paused by you), admin-paused shows 'Admin locked' instead of resume button. Admin blade: paused badge shows reason too.
- Tests: partner pause->resume instant active (paid); admin pause blocks partner resume; budget exhausted blocks; ended blocks. php -l + view:cache clean.

## Audit round 2: pause/resume + max_impressions + admin/partner UI fixes
- AdController::applySpend -> now ALSO auto-pauses (paused_by=system) when max_impressions reached (was: budget-only).
- Admin update(): setting status=active runs cannotRunReason guard (reverts to pending/paused + error); paused_by cleared when leaving paused status.
- Refund: sets paused_by='admin' so partner sees reason + Admin locked (no bogus resume button).
- Partner resume error: removed 'Top up' (no top-up flow exists) -> 'Create a new campaign to run more ads.'
- Partner destroy(): blocked when paid_amount > spent_amount (money owed -> contact admin).
- Blades (partner+admin): active campaigns that are ended or budget-exhausted now show 'Ended/Budget exhausted - no longer serving' (was green 'Active' misleading).
- Partner blade: Edit button hidden for paid campaigns (update() blocks paid edits anyway).
- Tests (standalone bootstrap): all 7 PASS - max_impressions pause, admin guard, refund paused_by, delete block, resume block, paused_by clear. php -l clean, view:cache OK.

## Payment gateway sandbox test round 1
- Found + fixed REAL bug: eSewa verifyESewa was POST -> 405. eSewa status API v2 needs GET + query params + transaction_uuid. Fixed in PaymentGatewayService::verifyESewa (added  param, array_filter) + AdController::esewaCallback passes transaction_uuid.
- Tests: T1 form fields PASS (amount 500.00 correct), T2 signature roundtrip + tamper rejection PASS, T3 sandbox API accepts + fake txn rejected PASS, T5 markPaid flow PASS (payment success -> campaign paid -> stays pending for admin).
- Khalti: keys EMPTY in .env -> initiate returns 401 clean. User said they will provide test keys (developer.khalti.com). e2e still needs ngrok (APP_URL=localhost:8000, callbacks unreachable).
- Live swap: just change .env keys (ESEWA_MERCHANT_CODE/SECRET_KEY/BASE_URL, KHALTI_SANDBOX/SECRET_KEY/PUBLIC_KEY/URLs) - code is env-driven.

## eSewa ES104 fix (Invalid payload signature) - CRITICAL
- Bug: eSewaSignature used values-only string (100,11-201-13,EPAYTEST). eSewa v2 requires field=value: total_amount=100,transaction_uuid=11-201-13,product_code=EPAYTEST. Fixed in eSewaSignature + verifyESewaSignature (PaymentGatewayService.php).
- Form also missing 'amount' field + had bogus 'merchant_code' - fixed (all 11 official fields now).
- Verified against OFFICIAL eSewa docs vector: total_amount=110,transaction_uuid=241028,product_code=EPAYTEST -> i94zsd3oXF6ZsSr/kGqT4sSzYQzjj1W/waxjWyRwaME= EXACT MATCH.
- TEST CREDENTIALS (official, developer.esewa.com.np):
  - eSewa: ID 9806800001-9806800005 (or 9711111111-4), Password Nepal@123, MPIN 1122, OTP/Token 123456, Merchant EPAYTEST, Secret 8gBm/:&EnhH.1/q
  - Khalti: ID 9800000000-9800000005, MPIN 1111, OTP 987654, keys from test-admin.khalti.com (live_secret_key)
- Browser redirect to localhost success_url works (client-side 302) - ngrok NOT strictly needed for e2e.

## eSewa e2e simulation - ALL PASS
- T-A forged callback REJECTED (status API catches fake txn -> payment failed, campaign unpaid) - security verified.
- T-B verified -> markPaid -> payment success, campaign paid, stays pending (admin approval).
- T-C admin approve -> ACTIVE serving.
- Live form POST to rc-epay.esewa.com.np returns HTTP 200 + login page (signature accepted, ES104 resolved).
- eSewa sandbox login currently flaky ("Service is currently unavailable" on 9806800001) - alternates: 9711111111-4 / Nepal@123 / OTP 123456; incognito + retry advised. Khalti keys STILL pending from user.

## Reward offers - root cause + fixes (Flutter shows nothing)
- ROOT CAUSE: DB had only 1 offer (id=6, expired Aug 8 21:40). API active() scope returned 0 -> app empty. Flutter side was COMPLETE all along (home _RewardsStrip, OffersScreen, StoreScreen /store, provider->getOffers). Extended offer #6 ends_at to +7d so app shows it.
- BUG: RewardOffer fillable was missing value_npr (form+validation had it) -> silently dropped, admin commission/earnings always 0. FIXED.
- Admin OfferController: approve/resume now guarded by cannotRunReason (ends_at passed -> 'the offer has already ended.'). Partner OfferController::resume same guard.
- Blades admin+partner offers: 'Ended' badge + red date for expired approved/paused offers.
- Tests PASS: value_npr saved, expired approve blocked, valid offer served by API. php -l + view:cache clean.

## TIMEZONE FIX - system was UTC, user in Nepal (UTC+05:45) - CRITICAL
- Root cause of 'offer not showing': PHP/Laravel ran UTC (10:33) while user entered Nepal wall-clock (16:04). now() UTC vs stored Nepal time -> scheduled offers appeared 'not yet started'. Windows was on Kathmandu tz already.
- FIXED: config/app.php 'timezone' => env('APP_TIMEZONE','UTC'); .env APP_TIMEZONE=Asia/Kathmandu. config/database.php mysql 'timezone' => env('DB_TIMEZONE','+05:45'); .env DB_TIMEZONE=+05:45 (MySQL session time_zone).
- Now: php now()=16:20 Asia/Kathmandu, mysql session +05:45. Stored datetimes were Nepal wall-clock all along (no conversion happened) so existing data is now CORRECT; no data migration needed.
- Verified: API offers count=2 (20% OFF shoes + BOGO pizza), ad campaigns active=2. App should show both offers now.
- NOTE: remember existing datetime-local inputs/values are stored as-is (wall clock) - consistent now.

## OFFER EXPIRE = SYSTEM LOCK (done, all verified)
Requirement: expired offer -> NOT shown in app; admin+partner see status 'paused by system', unchangeable (no edit/resume/approve) once ended.
- Migration `2026_08_09_160000_add_paused_by_to_reward_offers_table` (paused_by) — RAN.
- RewardOffer: fillable paused_by; isEnded(); isSystemLocked() (paused_by==='system'); static expireEnded() sweeps approved + ends_at<=now -> status=paused, paused_by=system. Called in Api/Admin/Partner OfferController::index + API claim, plus artisan `offers:expire`.
- Guards: Admin cannotRunReason blocks system-locked ('the offer has ended and is locked by the system.'); approve/resume clear paused_by; pause sets paused_by='admin'. Partner update/pause/resume blocked when isSystemLocked(); pause sets paused_by='partner'.
- Blades admin+partner offers: status cell shows 'Ended — locked by system' / 'Paused by admin' / 'Paused by you'; Resume hidden for system-locked (shows 'Locked'); Edit hidden for system-locked (partner).
- Flutter: OfferProvider.fetchOffers filters .where(!o.isExpired) - client-side safety (covers home strip, store rewards, offers screen).
- Schedule: routes/console.php `Schedule::command('offers:expire')->everyMinute()->withoutOverlapping()`.
- Verified: php -l clean x7, migrate OK 177ms, view:cache clean, Tinker tests T1-T5 ALL PASS (expired auto-paused+hidden from API, claim blocked, system lock true, admin resume blocked, command ran), flutter analyze 0 issues. Test rows cleaned.
- PENDING: Khalti keys still never received from user (said 'dinchhu' but no show) — needed for Khalti e2e. eSewa sandbox occasionally 'service unavailable' (alt creds 9711111111-4 / Nepal@123 / OTP 123456, incognito+retry; signature vector verified).

## QUEUE WORKER WAS NOT RUNNING - review AI not auto-processing (FIXED)
- User asked 'review ai auto chaleko xa?'. FINDING: NO queue:work process (only artisan serve x2). Startup `LaravelQueueWorker.vbs` was one-shot (Run ..,0,False) - worker had died silently; no restart. jobs=0 (nothing backlogged because place_reviews=0, but ANY new review would sit forever).
- AI pipeline itself was fine: GROQ_API_KEY set (AI_PROVIDER=groq), ModerateReview dispatched from PlaceController.php:796 on review submit, job writes moderation_status/moderated_at on place_reviews (cols confirmed incl. moderated_by).
- LIVE TEST PASS: created test review #1 (pending) -> dispatch ModerateReview -> worker picked up in ~2s -> moderation_status=approved, moderated_at=16:55:53 (real Groq call). Cleaned up.
- FIX: LaravelQueueWorker.vbs rewritten as self-healing loop (Run ...,0,True + Sleep 5000 in Do/Loop) -> restarts queue:work whenever it exits. VERIFIED: killed worker PID -> respawned in <12s.
- Worker started NOW via wscript (PID wscript + queue:work child). Will auto-start at every login via Startup folder.
- NOTE: `schedule:work` is NOT running either - offers:expire everyMinute won't fire; BUT it's covered lazily (expireEnded() runs on every offers list/claim API + admin/partner index). Consider schedule:work VBS if other scheduled jobs matter later.
- Old failed_jobs row (2026-07-17 TranslateContent: model_translations 'value' NOT NULL, create lacked value) - ALREADY FIXED in code (line 65 has 'value' => $translated). Historical only.
- Gotcha: temp PHP test scripts need forceCreate for PlaceReview (uuid in $guarded); users table has NO 'role' column.

## PAYOUT SYSTEM + value_npr LOCK + ROUNDING (done, tests ALL PASS)
- User asked how Earned(Rs.) works -> explained -> user said 'gardeu' for all 3 suggestions.
- PAYOUTS: migration 2026_08_09_170000_create_payouts_table (travel_partner_id, amount, status pending/paid/rejected, payment_method esewa/khalti/bank, payment_detail, note, admin_note, requested_at, processed_at, processed_by). Model Payout. Admin sidebar = permission-driven: inserted permission manage_payouts (menu Payouts, monetization group, order 160) + granted to admin role 3 (insertOrIgnore).
- Balance math (TravelPartner helpers): offerEarned() = sum partner_earnings status=used; payoutsHeld() = pending+paid; payoutBalance() = earned - held. AVAILABLE = earned - (pending+paid) - verified semantics.
- Partner: /partner/payouts (balance cards + request form + history + cancel pending), Partner\PayoutController (store validates amount <= balance via back() error, not validation rule).
- Admin: /admin/payouts (stats + filters + Mark Paid modal + Reject modal w/ required reason), Admin\PayoutController (requireAdmin pattern like Admin\OfferController; markPaid/reject only for pending; audit log payout.paid/payout.rejected).
- value_npr LOCK: migration 2026_08_09_170100 value_npr_locked bool. Admin approve() sets locked=true; new admin route POST /admin/offers/{offer}/value (updateValue - inline form in admin offers table, Value (Rs.) column added); Partner\OfferController::update preserves old value_npr if locked; partner offer_form disables input + lock note.
- ROUNDING: partner/offers.blade earned column now 2 decimals; dashboard added Available Balance gradient banner (click -> payouts).
- Tests: payout_test.php 12/12 PASS (approve locks, partner edit preserves, admin changes, balance math 540-500=40, over-balance blocked, paid/reject/cancel flows, rejected not held). Cleanup verified (0 rows). view:cache OK. Permission row id=26 granted role 3.
- Note: value_npr_lock survives pause->edit->resume (that was the gap; approved-guard alone didn't cover paused edit).
- Payout payouts()/offerEarned() helpers live on TravelPartner model (not OfferRedemption) - reuse for admin partner pages later.

## PER-METHOD PAYOUT MINIMUMS + GATEWAY KEYS FROM ADMIN SETTINGS (done, 20/20 tests PASS)
- User asked for payout system -> 'gardeu' -> built full payout system + value_npr lock + rounding (previous section), THEN this round: per-method minimums + gateway keys admin-editable.
- Migration 2026_08_09_180000_add_payout_minimum_and_gateway_settings: seeds payout_min_esewa=100, payout_min_khalti=100, payout_min_bank=500, gateway_esewa_merchant_code/secret_key/sandbox, gateway_khalti_secret_key/public_key/sandbox (env() defaults, sandbox flags '1'/'0' strings). GOTCHA: PHP class CONST can't call env() -> used private method defaults() instead.
- PaymentGatewayService now reads gateway_* settings from game_settings (falls back to config/payments.php): eSewaForm/verifyESewa/eSewaSignature use settings merchant code+secret+sandbox toggle; initiateKhalti/verifyKhalti same. Existing config() callers unaffected.
- PayoutController::store validation: amount >= per-method min (Rules\In apply) - esewa/khalti/bank mins from settings; blade shows live min hint + updates on method change (updateMin() JS); below-min rejected w/ error message.
- AdminController settings() + updateSettings() expose all 11 new keys (validation added); admin/settings.blade.php new fieldsets (Payout minimums + eSewa/Khalti gateway sections, save-as-string).
- Tests (threshold_test.php): seeds PASS, gateway service reads settings (form merchant code, sandbox/live base_url, signature with new secret) PASS, per-method min enforcement (50/80/200 blocked, 100/500 allowed) PASS, admin-raised min enforced (150 blocked, 250 allowed) PASS, updateSettings writes + settings() view exposes keys PASS. All 20 PASS, cleanup verified (test payouts/redemption/offer deleted, settings restored).
- NOTE: test caught ValidationException thrown (not RedirectResponse) for below-min - real HTTP request converts to redirect with errors automatically.

## ADMIN OFFERS: VALUE DISPLAY FIX + SOFT DELETE WITH PARTNER WARNING (done, 14/14 PASS)
- BUG: admin/offers value input used number_format(...,2) which inserts comma (1,500.00) -> browser type=number rejects it -> input showed EMPTY for values >= 1000. FIXED: number_format(...,2,'.','') -> 1500.00 renders correctly.
- SOFT DELETE: migration 2026_08_09_190000 (softDeletes + admin_removed_reason on reward_offers, RAN). RewardOffer uses SoftDeletes + fillable admin_removed_reason.
- Admin: destroy(reason required min:3, stored as admin_removed_reason, soft delete, log offer.deleted); restore (route ->withTrashed(), clears reason, log offer.restored); index ?deleted=1 tab (onlyTrashed, stats deleted count); blade: Deleted tab with count, delete button + modal (required 'Message for partner'), Restore button, Removed badge+reason on title, value shown as plain text when trashed.
- Partner: index now ->withTrashed() so removed offers VISIBLE with red 'Removed by admin - {reason}' warning; all actions hidden for trashed rows (redemptions view still allowed); destroy on trashed -> 404.
- API/claim flows unaffected (global scope auto-excludes trashed from active()).
- GOTCHA: tests passed deleted=1 as int -> strict === '1' failed; real HTTP always string. Tests: 14/14 PASS (value render, soft delete+reason, hidden from active, partner warning, destroy 404, reason required, restore, deleted tab). Cleanup verified.
